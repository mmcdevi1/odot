<%= render 'layouts/header' %>  
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert flash <%= type %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
